# p3 Manifests

This directory is monitored by Argo CD (`p3/confs/app.yaml`).

Default image tag is `v1` in `deployment.yaml`.
To test rollout to `v2`, update the image tag to `wil42/playground:v2` and push.
